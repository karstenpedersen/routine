import { FlatList, StyleSheet, Text, View } from "react-native";
import React from "react";

export default function EmojiModal() {
  return (
    <View>
      <Text>EmojiModal</Text>
      <FlatList horizontal data={}></FlatList>
    </View>
  );
}

const styles = StyleSheet.create({});
